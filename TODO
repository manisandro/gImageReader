- showHelp -> showHelp(std::string& chapter)
Low prio:
- replace all ?
- markers in buffer to find corresponding page from cursor position
- packagekit is broken

Cleanup:
- const in private member functions?
- add GtkMenuButton in glade instead of in the code, as soon as glade supports it

win32: uninstall previous version during setup
win32: menu entries